# E2E Test Report — Run #005

- Run: **#005**
- Time: 2026-06-15
- Branch: `test/e2e-bdd-playwright`
- Command: `cd test && npm run test:e2e` (full regression)
- Result: ✅ **54 passed / 0 failed / 0 flaky** (54 total; **1 expected xfail**, see §4)
- Duration: ~3.6 min

## 1. Summary

After Phase 5 (admin extension TC-AD-03..11), case count grew from Run #004's 45 to **54**, adding admin session (TC-AD-03..06) and management (TC-AD-07..11). Full regression all green, no retries.

The only non-green item remains `Concurrent reviews of one request let only one win`, tagged `@fail` (xfail) for the backend concurrency defect (§4–§5). Playwright counts it as passed.

## 2. Changes Since Run #004 (Phase 5)

- **Admin session (`features/admin/session.feature`, TC-AD-03..06)**: anonymous protected-route redirect; expired access token auto-refresh; invalid refresh clears tokens; dual-tab logout invalidation.
- **Admin management (`features/admin/management.feature`, TC-AD-07..11)**: filter by status/email; detail field checks; manual reschedule (success + duration mismatch rejected with DB check); business-day toggle + block create/delete (public availability reflects); availability negatives (block end<start, delete already-deleted block).
- Added `pages/AdminAvailabilityPage.ts`, `steps/admin-management.steps.ts`; extended `AdminBookingsPage.ts`, `admin.steps.ts`, `support/fixtures.ts`.

## 3. Results by Module

| Module | Source feature | Scenarios | Result |
| --- | --- | --- | --- |
| Admin — login | `admin/login.feature` | 2 | ✅ |
| Admin — session (TC-AD-03..06 + smoke) | `admin/session.feature` | 5 | ✅ |
| Admin — management (TC-AD-07..11) | `admin/management.feature` | 5 | ✅ |
| Booking — TC-BK-01..07/11/12 | `booking/booking.feature` | 17 | ✅ |
| Booking — concurrency & hold expiry (TC-BK-08/09/10) | `booking/concurrency.feature` | 3 | ✅ |
| Reschedule — TC-RS-01..11 | `booking/reschedule.feature` | 11 | ✅ (1 xfail) |
| Product order — TC-OD-01..06 | `order/product-order.feature` | 11 | ✅ |
| **Total** | | **54** | **54 passed / 0 failed** |

## 4. Known Gaps

- **TC-RS-10 (backend concurrency, xfail)**: `ApproveRescheduleRequestAsync` uses unlocked `SELECT` (no `FOR UPDATE`). Two concurrent approvals of the same pending request both succeed; exactly-one-wins only serially. Observed 2 OK vs expected 1; deterministic; `@fail` until locking is added.
- **TC-AD-11 "business-hours start>end" not automated**: backend does not validate hour order (only parseable times + day range, Phase 0.4). `management.feature` covers block `end<start` (400) and delete missing block (404); start>end left as known backend gap.

## 5. Stability Observations

First Phase 5 full run had 1 failure (`Admin manually reschedules a booking`) + 1 flaky (`Admin manages business hours and availability blocks`); **management.feature alone 5/5 stable**; full re-run 54/54 green, 0 flaky. Matches Run #003 §4: reused frontend dev server can blank after long HMR, causing occasional UI timeouts — not test logic.

- Recommendation: restart frontend dev server before long UI suites (or let Playwright start a fresh instance); keep `retries: 2`.

## 6. Notes

- Stripe fully mocked (`STRIPE_FAKE_PAYMENTS` + forged webhooks); browser blocks `stripe.com`.
- Stop dev backend before runs (tests use `bookingsys_test`).
